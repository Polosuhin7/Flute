{
    "name": "flute",
    "private": true,
    "devDependencies": {
        "lerna": "^4.0.0"
    },
    "engines": {
        "node": ">=10.16.0 <=14.x.x",
        "yarn": "1.*"
      },
    "scripts": {
        "bootstrap": "npx lerna bootstrap",
        "heroku-prebuild": "yarn bootstrap",
        "heroku-postbuild": "yarn build:web",
        "start": "npx lerna exec 'yarn start' --scope backend",
        "develop": "npx lerna exec 'yarn develop' --scope backend",
        "build:web": "npx lerna exec 'yarn build:web' --scope mobile"
    }
}
